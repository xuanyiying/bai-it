# OpenEn — 产品需求文档

## 用户痛点

英文阅读中最大的障碍不是单词，而是长句——多层嵌套的从句、倒装、插入语让人一读就晕。现有工具要么只做翻译（失去学习机会），要么只做词典（解决不了句子结构问题）。

## 设计原则

1. **装完即用**：用户装插件、填 API key，就能开始用。不需要注册、登录、部署后端
2. **数据归用户**：所有数据存浏览器本地，不上传、不泄露
3. **学习而非翻译**：帮用户理解句子结构，不是替用户读

## 功能范围

### P0：分块（核心价值）

- 插件直调 LLM API（Gemini 格式 + OpenAI 兼容格式）
- 本地规则引擎预过滤（简单句不浪费 API 调用）
- 分块结果渲染（L1-L5 五级分块粒度）
- 缓存机制（同一句子不重复调用 API）
- 全站点支持 + 主流站点专用选择器（X、Reddit、Medium 等）

### P1：学习系统

- 生词积累与管理
- 管理界面（插件 Options 页，React）
  - Dashboard：统计卡片 + 最近生词
  - 阅读记录：句子卡片列表
  - 生词本：搜索、过滤、标记掌握
  - 每日学习 + 本周回顾（本地调 LLM 生成）
- 已知词过滤

### P2：壁纸

- Options 页内 Canvas 渲染，浏览器端生成 PNG
- 批量生成 + 下载 ZIP
- 手动传输到手机（V1 不做云端传输）

## 不做的事情（V1）

- 云端同步、用户认证、登录系统
- 移动端原生应用
- 多语言翻译目标（V1 仅支持中文释义）
- Safari / Firefox 扩展
